# Python BLE Testing Requirements
# Install with: pip install -r requirements_test.txt

# BLE library for Python
bleak>=0.20.0

# Async support (usually included with Python 3.7+)
asyncio

# Logging and utilities (built-in)
# sys, logging are part of standard library
